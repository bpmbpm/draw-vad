# Детальное описание функций ARIS Express Clone

## Содержание
1. [Меню Файл](#меню-файл)
2. [Меню Правка](#меню-правка)
3. [Меню Вид](#меню-вид)
4. [Меню Модель](#меню-модель)
5. [Меню Формат](#меню-формат)
6. [Меню Инструменты](#меню-инструменты)
7. [Меню Справка](#меню-справка)

---

## Меню Файл

### Создать (Файл → Создать)
**Назначение:** Создает новую диаграмму с типом по умолчанию (VAD).

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.createNewDiagram()`
- Use Case: `CreateDiagramUseCase`

**Параметры:**
- Тип диаграммы берется из `AppConfig.notations.default`

**Действия:**
1. Вызывает `createDiagramUseCase.execute(type)`
2. Создает новый экземпляр `Diagram`
3. Отображает диаграмму на холсте через `CanvasController`
4. Обновляет статус-бар

---

### Открыть... (Файл → Открыть)
**Назначение:** Открывает сохраненную диаграмму из локального хранилища.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.openDiagram()`
- Service: `DiagramService.listDiagrams()`

**Действия:**
1. Запрашивает список сохраненных диаграмм через `DiagramService`
2. Показывает диалог выбора
3. Загружает выбранную диаграмму
4. Отображает на холсте

---

### Сохранить (Файл → Сохранить)
**Назначение:** Сохраняет текущую диаграмму.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.saveDiagram()`
- Use Case: `SaveDiagramUseCase`
- Repository: `DrawioRepository`

**Действия:**
1. Проверяет наличие открытой диаграммы
2. Валидирует диаграмму
3. Сохраняет через `SaveDiagramUseCase`
4. Данные сохраняются в localStorage в формате JSON
5. Обновляет временную метку изменения

---

### Сохранить как... (Файл → Сохранить как)
**Назначение:** Сохраняет диаграмму с новым именем.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.saveDiagramAs()`

**Действия:**
1. Запрашивает новое имя диаграммы
2. Обновляет `diagram.name`
3. Вызывает стандартную процедуру сохранения

---

### Импорт... (Файл → Импорт)
**Назначение:** Импортирует диаграмму из файла draw.io XML.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.importDiagram()`
- Parser: `DrawioXmlParser`

**Форматы:**
- .drawio (XML)
- .xml

**Действия:**
1. Показывает диалог выбора файла
2. Читает содержимое файла
3. Парсит XML через `DrawioXmlParser.xmlToDiagram()`
4. Создает доменные объекты (Diagram, DiagramElement)
5. Отображает импортированную диаграмму

**Парсинг:**
- Извлекает mxCell элементы
- Определяет тип диаграммы по стилям элементов
- Восстанавливает иерархию родитель-потомок
- Создает связи между элементами

---

### Экспорт... (Файл → Экспорт)
**Назначение:** Экспортирует диаграмму в файл draw.io.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.exportDiagram()`
- Parser: `DrawioXmlParser.diagramToXml()`

**Форматы экспорта:**
- .drawio (основной формат)
- .xml (совместимый формат)

**Действия:**
1. Конвертирует доменную модель в XML
2. Создает mxGraphModel структуру
3. Генерирует blob с XML
4. Инициирует скачивание файла

---

### Печать... (Файл → Печать)
**Назначение:** Печатает текущую диаграмму.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.printDiagram()`

**Действия:**
1. Вызывает `window.print()`
2. Использует браузерный диалог печати

---

### Выход (Файл → Выход)
**Назначение:** Закрывает приложение.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.exit()`

**Действия:**
1. Показывает подтверждение
2. Закрывает окно браузера

---

## Меню Правка

### Отменить (Правка → Отменить)
**Назначение:** Отменяет последнее действие.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.undo()`
- Стек: `undoStack`

**Примечание:** Базовая реализация готова для расширения.

---

### Повторить (Правка → Повторить)
**Назначение:** Повторяет отмененное действие.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.redo()`
- Стек: `redoStack`

---

### Вырезать / Копировать / Вставить / Удалить (Правка → ...)
**Назначение:** Стандартные операции буфера обмена.

**Реализация:**
- Файл: `js/app.js`
- Методы: `cut()`, `copy()`, `paste()`, `deleteSelected()`

---

### Выделить все (Правка → Выделить все)
**Назначение:** Выделяет все элементы на диаграмме.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.selectAll()`

---

## Меню Вид

### Увеличить (Вид → Увеличить)
**Назначение:** Увеличивает масштаб диаграммы на 25%.

**Реализация:**
- Файл: `js/presentation/ui/CanvasController.js`
- Метод: `CanvasController.zoomIn()`

**Действия:**
1. Увеличивает `zoomLevel` на 25%
2. Применяет CSS transform: scale()
3. Обновляет индикатор масштаба

**Ограничения:**
- Минимум: 25%
- Максимум: 400%

---

### Уменьшить (Вид → Уменьшить)
**Назначение:** Уменьшает масштаб диаграммы на 25%.

**Реализация:**
- Файл: `js/presentation/ui/CanvasController.js`
- Метод: `CanvasController.zoomOut()`

---

### По размеру страницы (Вид → По размеру страницы)
**Назначение:** Подгоняет диаграмму под размер окна.

**Реализация:**
- Файл: `js/presentation/ui/CanvasController.js`
- Метод: `CanvasController.zoomToFit()`

**Действия:**
1. Вычисляет оптимальный масштаб
2. Устанавливает масштаб для полного отображения

---

### 100% (Вид → 100%)
**Назначение:** Устанавливает масштаб 100% (актуальный размер).

**Реализация:**
- Файл: `js/presentation/ui/CanvasController.js`
- Метод: `CanvasController.zoomToActual()`

---

### Сетка (Вид → Сетка)
**Назначение:** Включает/выключает отображение сетки.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.toggleGrid()`
- Конфигурация: `AppConfig.ui.grid.enabled`

**Параметры сетки:**
- Размер: 20px (настраивается в `AppConfig.ui.grid.size`)
- Цвет: #e0e0e0 (настраивается в `AppConfig.ui.grid.color`)

---

### Привязка к сетке (Вид → Привязка к сетке)
**Назначение:** Включает/выключает привязку элементов к сетке.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.toggleSnap()`
- Конфигурация: `AppConfig.ui.grid.snapToGrid`

---

### Панели инструментов... (Вид → Панели)
**Назначение:** Настройка отображения панелей.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.showPanelSettings()`

---

## Меню Модель

### Новая VAD диаграмма (Модель → Новая VAD диаграмма)
**Назначение:** Создает новую диаграмму VAD.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.createDiagram('vad')`
- Нотация: VAD (Value Added Diagram)

**Трафареты VAD:**
- Базовый процесс (Base Process) - #B9E0A5
- Детализированный процесс (Detail Process) - #B9E0A6
- Внешний процесс (External Process) - #D4E1F5
- Примечание (Note)

---

### Новая EPC диаграмма (Модель → Новая EPC диаграмма)
**Назначение:** Создает новую диаграмму EPC.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.createDiagram('epc')`
- Нотация: EPC (Event-driven Process Chain)

**Трафареты EPC:**
- Событие (Event) - шестиугольник
- Функция (Function) - прямоугольник
- Соединитель (Connector) - ромб
- Организационная единица (Org Unit) - эллипс

---

### Новая организационная диаграмма (Модель → Новая организационная диаграмма)
**Назначение:** Создает организационную схему.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.createDiagram('org')`

**Трафареты:**
- Должность (Position)
- Подразделение (Department)
- Сотрудник (Person)

---

### Свойства модели... (Модель → Свойства модели)
**Назначение:** Показывает информацию о текущей диаграмме.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.showModelProperties()`

**Отображаемая информация:**
- Название диаграммы
- Тип нотации
- Количество элементов
- Дата создания
- Дата изменения
- Метаданные

---

### Проверить модель (Модель → Проверить модель)
**Назначение:** Валидирует диаграмму согласно правилам нотации.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.validateModel()`
- Service: `DiagramService.validateDiagram()`
- Domain: `Diagram.validate()`

**Правила валидации:**

**VAD:**
- Проверка наличия исполнителя (role) у процессов
- Проверка корректности именования процессов

**EPC:**
- Проверка чередования событий и функций
- Проверка использования соединителей

**Результат:**
- Список ошибок (errors)
- Список предупреждений (warnings)
- Статус валидности (isValid)

---

## Меню Формат

### Формат фигуры... (Формат → Формат фигуры)
**Назначение:** Изменяет стиль выбранной фигуры.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.formatShape()`

**Настраиваемые свойства:**
- Цвет заливки (fillColor)
- Цвет границы (strokeColor)
- Толщина границы (strokeWidth)

---

### Формат линии... (Формат → Формат линии)
**Назначение:** Изменяет стиль соединительных линий.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.formatLine()`

---

### Формат текста... (Формат → Формат текста)
**Назначение:** Изменяет форматирование текста.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.formatText()`

**Настраиваемые свойства:**
- Размер шрифта (fontSize)
- Семейство шрифта (fontFamily)
- Цвет текста (fontColor)

---

### Выравнивание / Распределение (Формат → ...)
**Назначение:** Выравнивает и распределяет элементы.

**Реализация:**
- Файл: `js/app.js`
- Методы: `align()`, `distribute()`

---

### На передний/задний план (Формат → ...)
**Назначение:** Изменяет порядок наложения элементов.

**Реализация:**
- Файл: `js/app.js`
- Методы: `bringToFront()`, `sendToBack()`

---

## Меню Инструменты

### Настройки... (Инструменты → Настройки)
**Назначение:** Открывает диалог настроек приложения.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.showSettings()`

**Настройки:**
- Язык интерфейса
- Тема оформления
- Параметры сетки
- Автосохранение

---

### Конфигурация... (Инструменты → Конфигурация)
**Назначение:** Редактирует конфигурационный файл.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.showConfiguration()`
- Конфигурация: `config/app-config.js`

---

## Меню Справка

### Содержание справки (Справка → Содержание справки)
**Назначение:** Показывает главную страницу справки.

**Реализация:**
- Файл: `js/presentation/ui/HelpController.js`
- Метод: `HelpController.showHelp('index')`

**Разделы справки:**
- О программе
- Поддерживаемые нотации
- Основные функции
- Начало работы

---

### Справка по VAD (Справка → Справка по VAD)
**Назначение:** Показывает подробную справку по нотации VAD.

**Реализация:**
- Файл: `js/presentation/ui/HelpController.js`
- Метод: `HelpController.showHelp('vad')`

**Содержание:**
- Описание нотации
- Элементы нотации
- Свойства процессов VAD
- Правила моделирования
- Примеры использования

---

### Справка по EPC (Справка → Справка по EPC)
**Назначение:** Показывает справку по нотации EPC.

**Реализация:**
- Файл: `js/presentation/ui/HelpController.js`
- Метод: `HelpController.showHelp('epc')`

---

### Справка по орг. схемам (Справка → Справка по орг. схемам)
**Назначение:** Показывает справку по организационным диаграммам.

**Реализация:**
- Файл: `js/presentation/ui/HelpController.js`
- Метод: `HelpController.showHelp('org')`

---

### О программе (Справка → О программе)
**Назначение:** Показывает информацию о приложении.

**Реализация:**
- Файл: `js/app.js`
- Метод: `ArisExpressApp.showAbout()`
- Конфигурация: `AppConfig.app`

**Отображаемая информация:**
- Название приложения
- Версия
- Описание
- Автор

---

## Панель инструментов

Быстрый доступ к основным функциям через иконки:

| Иконка | Функция | Метод |
|--------|---------|-------|
| 📄 | Создать | `createNewDiagram()` |
| 📂 | Открыть | `openDiagram()` |
| 💾 | Сохранить | `saveDiagram()` |
| ↶ | Отменить | `undo()` |
| ↷ | Повторить | `redo()` |
| 🔍+ | Увеличить | `zoomIn()` |
| 🔍- | Уменьшить | `zoomOut()` |
| ⊡ | По размеру | `zoomFit()` |

---

## Панель трафаретов

### Проводник моделей
**Назначение:** Отображает дерево моделей и диаграмм.

**Реализация:**
- Файл: `js/presentation/ui/StencilController.js`
- Библиотека: jstree

---

### Трафареты
**Назначение:** Библиотека элементов для выбранной нотации.

**Реализация:**
- Файл: `js/presentation/ui/StencilController.js`
- Метод: `StencilController.loadStencils(notationType)`
- Service: `NotationService.getStencils()`

**Действия:**
1. Выбор нотации из выпадающего списка
2. Загрузка трафаретов для нотации
3. Отображение элементов
4. Drag & drop или клик для добавления

---

## Панель свойств

**Назначение:** Редактирование свойств выбранного элемента.

**Реализация:**
- Файл: `js/presentation/ui/PropertiesController.js`
- Метод: `PropertiesController.showElementProperties(element)`

**Свойства:**

**Общие:**
- ID (только чтение)
- Тип (только чтение)
- Название
- Позиция (X, Y)
- Размер (ширина, высота)

**VAD-специфичные:**
- Исполнитель (Role)
- Комментарий

**Действия:**
1. Выбор элемента на холсте
2. Отображение свойств в панели
3. Редактирование значений
4. Автоматическое обновление при изменении

---

## Архитектура и реализация

### Domain-Driven Design (DDD)

**Domain Layer (Доменный слой):**
- `Diagram` - агрегат диаграммы
- `DiagramElement` - элемент диаграммы
- `Model` - модель (коллекция диаграмм)
- `Point`, `Size`, `Style` - value objects

**Application Layer (Слой приложения):**
- `DiagramService` - сервис работы с диаграммами
- `ModelService` - сервис работы с моделями
- `NotationService` - сервис работы с нотациями
- Use Cases: `CreateDiagram`, `SaveDiagram`, `LoadDiagram`

**Infrastructure Layer (Инфраструктурный слой):**
- `DrawioRepository` - репозиторий диаграмм
- `LocalStorageAdapter` - адаптер localStorage
- `DrawioXmlParser` - парсер draw.io XML

**Presentation Layer (Слой представления):**
- `MenuController` - контроллер меню
- `ToolbarController` - контроллер панели инструментов
- `CanvasController` - контроллер холста
- `StencilController` - контроллер трафаретов
- `PropertiesController` - контроллер свойств
- `HelpController` - контроллер справки

---

## Конфигурация

Все настройки хранятся в `config/app-config.js`:

- Метаданные приложения
- Настройки UI
- Конфигурация нотаций (VAD, EPC, Org, BPMN)
- Настройки файлов
- Правила валидации
- Настройки инструментов рисования
- Система справки
- Интеграция с GitHub
- Режим разработчика

Конфигурация загружается при старте и доступна через глобальный объект `AppConfig`.
